# Minimal diagnostic workflow — if THIS startup-fails, the problem is account/runner-level, not our files.
name: canary
on:
  push:
    branches: [master]
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "canary alive"
